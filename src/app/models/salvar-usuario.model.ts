export interface CriarUsuarios{ 
    id: number
    nome: string
    email: string
    telefone: number
}

export interface CriarFilmes{ 
    id: number
    nome: string
    genero: number
}

export interface CriarGenero{ 
    id: number
    genero: string
}
